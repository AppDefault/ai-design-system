**Avatar** — gradient tile with a serif initial (default) or an image.

```jsx
<Avatar initial="A" size={40} />
<Avatar initial="M" size={38} shape="circle" />   {/* user / persona */}
<Avatar src="/annie.jpg" size={64} />
```

Rounded-rect by default (radius scales with size); use `shape="circle"` for people/personas.
