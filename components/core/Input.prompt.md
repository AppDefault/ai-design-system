**Input** — single-line text field. Subtle dark fill, hairline border, gold focus ring.

```jsx
<Input placeholder="Search characters by name or mood…" icon={<span>⌕</span>} />
<Input placeholder="Character name" />
```

Pass `icon` for a leading glyph (search). Border warms to gold on focus.
