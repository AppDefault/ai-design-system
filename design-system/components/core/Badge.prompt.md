**Badge** — small overlay/label markers.

```jsx
<Badge variant="pro">PRO</Badge>
<Badge variant="stat">1.8M</Badge>
<Badge variant="premium">✦ Premium</Badge>
```

`pro` sits top-left on paid character cards; `stat` overlays counts on imagery (top-right, dark scrim); `premium` is the nav upgrade pill.
