# AIGirl — Noir Editorial Design System

Tokens: `tokens.json`. Living component reference + interactive demo: `Design System.dc.html` and `AIGirl Prototype.dc.html` (open directly in a browser).

Copy these files into your repo's `design-system/` folder to publish.
